# Build output
build*/
.cache/
aqtinstall.log

# Machine-specific CMake preset overrides (absolute Qt/SDK/NDK paths)
CMakeUserPresets.json

# Local tooling and assistant config — never committed
CLAUDE.md
COLLABORATION.md
.claude/

# Scratch experiments, not part of the build
leak.cpp
